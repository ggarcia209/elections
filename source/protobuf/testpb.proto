syntax = "proto3";

package protobuf;

message Outer {
    string Name = 1;
    string Place = 2;

    message Inner {
        int32 Score = 1;
        string Grade = 2;
    }

    repeated Inner Scores = 3;

    repeated repeated string TupleList = 4;
}